# Fork-simulation RPC endpoints. Copy to `.env` (gitignored) and fill in.
# An ARCHIVE endpoint is required to pin historical blocks (e.g. to replay a past hack).
ETH_RPC_URL=https://eth-mainnet.g.alchemy.com/v2/YOUR_KEY
